 pipeline {
    agent any
    stages {
        stage('Terraform Deploy') {
           steps {
               dir('/home/serega/project/terraform') {
                    sh 'terraform init'
                    sh 'terraform plan'
                    sh 'terraform apply -auto-approve'
                }
            }
        }
 }
 }